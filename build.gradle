group 'jihedamine'
version '1.0-SNAPSHOT'

subprojects {
    apply plugin: 'java'
    apply plugin: "maven"
    sourceCompatibility = 1.9

    repositories {
        jcenter()
        mavenLocal()
    }

    dependencies {
        testCompile 'org.junit.jupiter:junit-jupiter-api:5.0.1'
    }
}
